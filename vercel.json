{
  "version": 2,
  "builds": [
    { "src": "client/next.config.js", "use": "@vercel/next" },
    { "src": "server/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/.*", "dest": "server/src/server.ts" },
    { "src": "/(.*)", "dest": "client/pages/$1" }
  ]
}
